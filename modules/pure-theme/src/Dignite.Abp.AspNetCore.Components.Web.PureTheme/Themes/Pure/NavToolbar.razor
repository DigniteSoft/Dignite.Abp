<ul class="navbar-nav justify-content-end nav-toolbar">
    @foreach (var render in ToolbarItemRenders)
    {
        @render
    }
</ul>

<BarMenu>
    <BarEnd>
        @foreach (var render in ToolbarItemRenders)
        {
                <BarItem>
            @render
                </BarItem>
        }
    </BarEnd>
</BarMenu>